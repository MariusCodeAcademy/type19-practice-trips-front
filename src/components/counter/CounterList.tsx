import React from 'react';
import SingleCounter from './SingleCounter';

export default function CounterList() {
  return (
    <div>
      CounterList
      <SingleCounter />
      <SingleCounter />
      <SingleCounter />
      <SingleCounter />
    </div>
  );
}
