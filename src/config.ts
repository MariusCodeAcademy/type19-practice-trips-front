export const beBaseUrl = 'http://localhost:3000';
export const tripsUrl = `${beBaseUrl}/trips`; // trips/deleted

export const countriesUrl = `${beBaseUrl}/countries`;
export const usersUrl = `${beBaseUrl}/users`;
